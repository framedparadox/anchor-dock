namespace Anchor;

/// <summary>
/// Shared constant for the icon picker's swatch grid (built in <c>DockWindow.Dialogs.cs</c>,
/// which every "Change icon…" entry point calls into directly). Partial of
/// <see cref="DockWindow"/>.
/// </summary>
public sealed partial class DockWindow
{
    private const int IconPickerColumns = 6;
}
