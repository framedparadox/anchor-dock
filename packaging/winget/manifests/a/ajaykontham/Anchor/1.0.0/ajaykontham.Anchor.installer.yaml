# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.9.0.schema.json
PackageIdentifier: ajaykontham.Anchor
PackageVersion: 1.0.0
Platform:
  - Windows.Desktop
MinimumOSVersion: 10.0.17763.0
InstallerType: zip
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: Anchor.exe
    PortableCommandAlias: anchor
UpgradeBehavior: install
Installers:
  - Architecture: x64
    # PLACEHOLDER — replace <owner>/<repo> with the real GitHub repo and upload the exact zip at
    # dist\Anchor-win-x64-1.0.0.zip as the v1.0.0 Release asset (keep the file name so this resolves).
    InstallerUrl: https://github.com/framedparadox/anchor-dock/releases/download/v1.0.0/Anchor-win-x64-1.0.0.zip
    # SHA256 of dist\Anchor-win-x64-1.0.0.zip produced by scripts/package-release.ps1 -Version 1.0.0.
    # If you rebuild the zip, re-run the script and update this to the newly printed hash.
    InstallerSha256: 6403E47130C02D143FC9EFE8A9067F1F95160092EB5D3A9E59FE5007E3426C05
ManifestType: installer
ManifestVersion: 1.9.0
